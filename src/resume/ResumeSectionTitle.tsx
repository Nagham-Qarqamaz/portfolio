function ResumeSectionTitle({ text }: { text: string }) {
    return (
        <div className="border-b-2 border-b-black text-[24px] font-bold mb-4">
            {text}
        </div>
    );
}

export default ResumeSectionTitle;
